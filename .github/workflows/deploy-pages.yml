name: Deploy Blazor WASM to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build Blazor WASM
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore src/ZamAiMart.Client/ZamAiMart.Client.csproj

      - name: Publish Blazor WASM
        run: |
          dotnet publish src/ZamAiMart.Client/ZamAiMart.Client.csproj \
            -c Release \
            -o publish/client \
            --no-restore \
            -p:GHPages=true

      - name: Fix base href for GitHub Pages
        run: |
          # Replace base href for GitHub Pages subdirectory
          sed -i 's|<base href="/" />|<base href="/ZAM-AI-MART/" />|g' \
            publish/client/wwwroot/index.html

      - name: Copy 404.html for SPA routing
        run: cp publish/client/wwwroot/index.html publish/client/wwwroot/404.html

      - name: Add .nojekyll (disable Jekyll processing)
        run: touch publish/client/wwwroot/.nojekyll

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: publish/client/wwwroot

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
