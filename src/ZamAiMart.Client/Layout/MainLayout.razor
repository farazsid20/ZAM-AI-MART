@inherits LayoutComponentBase

<div class="min-h-screen bg-gray-50 dark:bg-gray-900 flex flex-col transition-colors duration-300" id="app-root">
    <NavMenu />
    <main class="flex-1">
        @Body
    </main>
    <Footer />
</div>

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            // Initialize dark mode from localStorage
        }
    }
}
